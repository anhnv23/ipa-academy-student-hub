"use client";
import {FormEvent,useEffect,useState} from "react";
import {supabase} from "../lib/supabase";
export function AccountManagement(){const[allowed,setAllowed]=useState(false);const[open,setOpen]=useState(false);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");useEffect(()=>{if(!supabase)return;(async()=>{const{data}=await supabase.auth.getUser();if(!data.user)return;const{data:p}=await supabase.from("profiles").select("role").eq("id",data.user.id).single();setAllowed(p?.role==="admin")})()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!supabase)return;setBusy(true);setMsg("");const form=new FormData(e.currentTarget);const{data}=await supabase.auth.getSession();const res=await fetch("/.netlify/functions/admin-users",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${data.session?.access_token||""}`},body:JSON.stringify({username:form.get("username"),password:form.get("password"),full_name:form.get("full_name"),role:form.get("role")})});const body=await res.json();setBusy(false);if(!res.ok)return setMsg(body.error||"Không thể tạo tài khoản.");setMsg(`Đã tạo tài khoản ${body.username}.`);e.currentTarget.reset()}if(!allowed)return null;
return <section className="panel account-admin"><div className="panel-head"><div><h3>Tài khoản đăng nhập</h3><p>Admin tạo username và mật khẩu cho Giáo viên/Học viên.</p></div><button className="primary" onClick={()=>setOpen(!open)}>{open?"Đóng":"Tạo tài khoản"}</button></div>{open&&<form className="account-form" onSubmit={submit}><label>Họ và tên<input name="full_name" required/></label><label>Vai trò<select name="role"><option value="teacher">Giáo viên</option><option value="student">Học viên</option></select></label><label>Username<input name="username" required minLength={3} pattern="[a-zA-Z0-9._-]+" autoCapitalize="none"/></label><label>Password<input name="password" type="password" required minLength={8}/></label><button className="primary" disabled={busy}>{busy?"Đang tạo…":"Lưu tài khoản"}</button></form>}{msg&&<p className="account-message">{msg}</p>}</section>}
